{
  "lexicon": 1,
  "id": "io.sunred.relay.getArticleStarCount",
  "defs": {
    "main": {
      "type": "query",
      "description": "Returns the globally accurate number of unique DIDs that starred an article URL, computed from all tracked repos.",
      "parameters": {
        "type": "params",
        "required": ["articleUrl"],
        "properties": {
          "articleUrl": {
            "type": "string",
            "description": "The article URL to query star count for."
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["articleUrl", "count"],
          "properties": {
            "articleUrl": {
              "type": "string"
            },
            "count": {
              "type": "integer",
              "description": "Number of unique DIDs with a star record for this article URL across all tracked repos."
            }
          }
        }
      }
    }
  }
}
