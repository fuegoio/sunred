{
  "lexicon": 1,
  "id": "io.sunred.relay.getUserSubscriptionCount",
  "defs": {
    "main": {
      "type": "query",
      "description": "Returns the globally accurate subscription count for a DID (number of feed subscription records authored by this DID across all tracked repos).",
      "parameters": {
        "type": "params",
        "required": ["did"],
        "properties": {
          "did": {
            "type": "string",
            "description": "The DID to query the subscription count for."
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["did", "count"],
          "properties": {
            "did": {
              "type": "string"
            },
            "count": {
              "type": "integer",
              "description": "Total number of feed subscription records authored by this DID across all tracked repos."
            }
          }
        }
      }
    }
  }
}
